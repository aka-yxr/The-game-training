#include <stdio.h>

/*
分析：
1.第一次想用结构体，没什么好的思路。第二次想用数组，也没什么好的思路。后来看了看别人的想法。豁然开朗。
2.数组的学习不能太死板，不仅仅要学会使用一维数组、二维数组、也应该能使用三维数组。
3.从这道题上，暴露了我对数组的认知还很浅薄。需要更多的学习和练习。
4.大概的思路就是用一个三维数组，三层循环。具体思路👇。
代码如下：

*/

int main()
{
    int stu[101][10][10];//三维数组 存储第i个学校 第j支队伍 的第k个队员
    int n,i,j,k;
    scanf("%d",&n);
    int team[100];//team容量不能太小，不然会有测试点过不去。
    int max=0;
    for(i=0;i<n;i++)
    {
        scanf("%d",&team[i]);
        if(team[i]>max)
        {
            max=team[i];
        }
    }
    int lasti=-1;
    int number=0;
    for(j=0;j<max;j++)//max==4；这一层循环，循环一次只排每个学校的10个人。
    {
        for(k=0;k<10;k++)//这一层就是每个学校的某一个队伍 的10个人   比如说j=0时，k=0，相当于排的是每个学校某一个队伍10个人中的第一名同学。
        {
            for(i=0;i<n;i++)//内层循环才为学校编号
            {
                if(j<team[i])//当j>=team[i]的时候，说明这个学校的人数已经排完了。
                            //如样例，第三个学校只有两个队伍，当j=2的时候，各个学校已经排了20个同学。第3个学校就不会继续排同学了。
                {
                    if(lasti==i)//说明只剩下一个学校了
                        number+=2;
                    else        //循环学校。
                        number++;
                    stu[i][j][k]=number;//给第i个学校，第j个队伍的队员排序
                    lasti=i;//每次更新，来用于最后的判断。
                }
            }
        }
    }
    //输出
    for(i=0;i<n;i++)
    {
        printf("#%d\n",i+1);
        for(j=0;j<team[i];j++)
        {
            for(k=0;k<9;k++)
                printf("%d ",stu[i][j][k]);
            printf("%d\n",stu[i][j][k]);
        }
    }
    
    return 0;
}
